---
title: "FuncNet"
layout: post
categories: Personal
tags: .NET Open&nbsp;Source
---

![FuncNet Logo](/assets/img/funcnet/logo.png)

<h2>{{ page.title }}</h2>

Open-source functional programming library for C#.


Open-source functional programming library for C#.

FuncNet is a source-generated library that brings functional programming concepts to C#, emphasizing ad-hoc unions,
Result and Option types, full monad implementations, and async support.
It enables railway-oriented programming patterns, making error handling and complex workflows more robust and maintainable.

[View on GitHub](https://github.com/pawelwilczewski/FuncNet)

## Examples

```csharp
private static Result<User, ValidationError, DatabaseError> CreateUser(
		string name,
		Func<string, Result<string, ValidationError>> validateName,
		Func<User, Result<User, DatabaseError>> saveToDb) =>
		Result<string, ValidationError>.FromSuccess(name)
			.BindSuccess(validateName)
			.MapSuccess(validatedName => new User(validatedName))
			.Extend<DatabaseError>()
			.BindSuccess(user => saveToDb(user)
				.Match(
					Result<User, ValidationError, DatabaseError>.FromSuccess,
					databaseError => databaseError));
```
<br>

```csharp
Union<int, string, bool> test = 213;
var test2 = test.Extend<int, string, bool, DateTime, decimal>();
var test3 = Union<int, bool, string, DateTime>.FromT1(false);
var test4 = Union<int, decimal, string, bool, DateTime>.FromT3(true);

// Note: This assignment works despite the order of union generic arguments
// This is possible thanks to source-generated conversions
test4 = test3;
```
